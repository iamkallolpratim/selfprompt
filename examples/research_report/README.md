# Example: Research Report Loop

Delegates to the `researcher` specialist via `--multi-agent`-style
orchestration, gathering findings across several turns before writing a
final report to disk.

```bash
pip install -e ../..
python run.py
```

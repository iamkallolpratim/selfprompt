# Example: Content Pipeline Loop

Draft -> self-critique -> revise, stopping once the loop's own confidence
crosses a threshold. Shows the critique step actually gating progress
instead of being decorative.

```bash
pip install -e ../..
python run.py
```
